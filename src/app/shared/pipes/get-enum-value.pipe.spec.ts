import { GetEnumValuePipe } from './get-enum-value.pipe';

describe('GetEnumValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GetEnumValuePipe();
    expect(pipe).toBeTruthy();
  });
});
